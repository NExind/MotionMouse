<!--
    Launcher icon foreground — adaptive icon layer.

    Adaptive icon foreground content must stay within the inner 66dp×66dp
    safe zone of the 108dp×108dp canvas (the outer ring may be cropped
    differently across device launchers — circle, squircle, rounded square).

    Shape mirrors the cursor motif used in ic_notification.xml for visual
    consistency, scaled up and centred within the safe zone.
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <path
        android:fillColor="#FFFFFF"
        android:pathData="M37,28 L37,64 L46,55 L55,75 L64,71 L55,51 L67,51 Z" />
</vector>
